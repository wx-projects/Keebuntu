# Third-party notices

The release archive includes the managed `dbus-sharp.dll` and
`dbus-sharp-glib.dll` assemblies so that distributions without packaged Mono
D-Bus bindings can load the plugin. These assemblies are built from the
`mono/dbus-sharp` and `mono/dbus-sharp-glib` projects and are distributed under
the MIT license. Their upstream license and copyright notices remain applicable.

GTK# is not bundled. It must be provided by the operating system.
